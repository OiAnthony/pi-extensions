{
  "name": "pi-packages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "pack:check": "bun scripts/check-packs.mjs",
    "release": "changeset publish",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
